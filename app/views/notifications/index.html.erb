<%# DIVE19_2_通知機能 で編集 %>  
<%# デフォルトは下記２行だけがある状態 どこで作られるのだろう？%>
<%# それを次のように書き換える というか、テキストコピペ%>
<h2>お知らせ</h2>
<ul>
  <% @notifications.each do |notification| %>
  <hr>
    <li>
      <span><%= notification.read %></span>
      <%# DIVE19_2_通知機能 で編集 %>  
      <%# 通知クリックで、通知を既読にする %>
      <%# 下記のように書き換える %>
      <p>
        <%= notification.comment.user.try(:name) %>さんが
        あなたのブログ（<%= link_to "#{notification.comment.blog.title}", blog_path(notification.comment.blog, notification_id: notification.id) %>）にコメントしました。
      </p>
      <p><%= notification.created_at %></p>      
      <%#
      <p>
        <%= notification.comment.user.try(:name) % >さんが
        あなたのブログ（<%= notification.comment.blog.title % >）にコメントしました。
      </p>
      <p><%= posted_time(notification.created_at) % ></p>
      %>
    </li>
  <% end %>
</ul>

<%#
<h1>Notifications#index</h1>
<p>Find me in app/views/notifications/index.html.erb</p>
%>